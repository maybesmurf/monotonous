overwrite: true
schema: "schema.graphql"
documents:
  - "apps/web/pages/**/!(*.d).{ts,tsx}"
generates:
  ./apps/web/graphql_client.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
    config:
      maybeValue: T
      nonOptionalTypename: true
