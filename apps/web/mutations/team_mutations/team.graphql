query Team($id: ID!) {
  team(id: $id) {
    id
    createdAt
    updatedAt
    name
    memberships {
      id
      user {
        id
        profile {
          id
          firstName
          lastName
        }
      }
    }
  }
}
