mutation ConfirmEmail(
  $token: String!
  $email: String!
  $firstName: String!
  $lastName: String!
) {
  confirmEmail(
    token: $token
    email: $email
    firstName: $firstName
    lastName: $lastName
  ) {
    id
  }
}
